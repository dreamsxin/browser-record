# Browser Record — 浏览器操作录制与回放系统设计方案

## 1. 项目目标
- **实时录制**：自动记录员工在浏览器中的所有操作（点击、输入、页面跳转等）。
- **可视化回放**：管理员通过时间轴预览操作过程，无需视频文件。
- **存储可控**：通过分段录制和循环覆盖，控制磁盘空间。
- **低侵入性**：员工无需额外操作，录制在后台静默进行。

---

## 2. 系统架构

系统由三部分组成：

| 组件 | 职责 |
|------|------|
| **客户端录制代理** | 运行在员工电脑上，连接浏览器，启动 Tracing，按 30 分钟分段保存文件并上传至服务器。 |
| **中央存储服务** | 接收文件，存储元数据，执行循环覆盖策略，提供文件下载接口。 |
| **管理后台** | 集成 Trace Viewer，供管理员查询、回放录制会话。 |

```
+------------------+     +-------------------+     +------------------+
|  员工浏览器       |     |  中央存储服务      |     |  管理后台        |
|  (CDP 端口)      |◀───▶|  (文件+数据库)    |◀───▶|  (Trace Viewer)  |
+------------------+     +-------------------+     +------------------+
        ▲                           ▲
        | (上传文件)                | (查询/下载)
        |                           |
+------------------+               |
| 客户端录制代理    |───────────────┘
| (Node.js 进程)   |
+------------------+
```

---

## 3. 仓库结构

本仓库采用 monorepo 结构（npm workspaces），包含三个包：

```
browser-record/
├── DESIGN.md                 # 本设计文档
├── README.md                  # 部署与运行说明
├── package.json               # workspaces 根配置
├── .gitignore
├── packages/
│   ├── recording-agent/       # 客户端录制代理（员工电脑）
│   │   ├── package.json
│   │   ├── config/
│   │   │   └── default.json
│   │   ├── src/
│   │   │   ├── index.js        # 入口
│   │   │   ├── recorder.js     # 连接浏览器 + 分段 Tracing
│   │   │   ├── uploader.js     # 异步上传 + 重试 + 本地缓存
│   │   │   ├── config.js       # 配置加载
│   │   │   └── health.js       # 健康检查 HTTP 接口
│   │   └── traces/             # 本地临时文件目录（运行时生成）
│   ├── storage-server/         # 中央存储服务
│   │   ├── package.json
│   │   ├── config/
│   │   │   └── default.json
│   │   ├── src/
│   │   │   ├── index.js        # Express 启动
│   │   │   ├── db.js           # SQLite 初始化 + 查询
│   │   │   ├── routes/
│   │   │   │   ├── upload.js    # 接收分段文件
│   │   │   │   ├── files.js     # 查询员工/会话/分段
│   │   │   │   ├── download.js  # 签名 URL 下载
│   │   │   │   └── auth.js      # JWT 登录/校验
│   │   │   ├── retention.js     # 循环覆盖清理
│   │   │   └── config.js
│   │   └── data/                # 文件 + SQLite（运行时生成）
│   └── admin-dashboard/         # 管理后台
│       ├── package.json
│       ├── src/
│       │   ├── server.js        # Express + EJS 视图
│       │   ├── views/
│       │   │   ├── login.ejs
│       │   │   ├── employees.ejs
│       │   │   ├── sessions.ejs
│       │   │   └── viewer.ejs
│       │   └── public/
│       │       └── app.css
│       └── config/
│           └── default.json
```

---

## 4. 客户端录制代理详细设计

### 4.1 运行环境
- 员工电脑安装 Node.js 运行环境（或打包为可执行文件）。
- 浏览器（Chrome/Edge）需以远程调试模式启动：`--remote-debugging-port=9222`（可通过启动脚本或快捷方式设置）。

### 4.2 核心录制逻辑

使用 Playwright 的 `connectOverCDP` 连接到本地浏览器，利用其 Tracing API 实现录制。

**关键配置**：
- `screenshots: true`（开启截图，用于时间线预览）
- `snapshots: false`（关闭 DOM 快照，大幅减小文件体积）
- `sources: false`（不包含源代码）

**分段录制流程**：
1. 代理启动后，连接到浏览器，获取默认上下文。
2. 启动 Tracing（`tracing.start`）。
3. 启动一个 30 分钟定时器（`setTimeout`）。
4. 定时器触发时：
   - 停止当前 Tracing，保存为 `.zip` 文件（命名规则：`{employeeId}_{sessionId}_{segmentIndex}_{timestamp}.zip`）。
   - 立即启动一个新的 Tracing（无缝衔接）。
   - 将刚保存的文件上传至中央服务器（异步，不阻塞新录制）。
5. 代理持续运行，直至员工关机或手动停止。

### 4.3 启动与监控
- 将代理设置为 Windows 服务或 Linux daemon，开机自启。
- 提供健康检查接口（如 HTTP `/health`），便于运维监控。

---

## 5. 中央存储与循环覆盖策略

### 5.1 存储结构
- **文件存储**：按员工 ID 分目录，例如：
  ```
  /data/recordings/
    ├── employee_001/
    │   ├── session_1700000000000/
    │   │   ├── segment_0_1700000000000.zip
    │   │   ├── segment_1_1700000300000.zip
    │   │   └── ...
    │   └── session_1700010000000/
    │       └── ...
    └── employee_002/
        └── ...
  ```
- **元数据库**（SQLite）记录每个文件的元数据：

  | 字段 | 类型 | 说明 |
  |------|------|------|
  | id | TEXT (UUID) | 文件唯一标识 |
  | employee_id | TEXT | 员工 ID |
  | session_id | TEXT | 会话 ID（开始时间） |
  | segment_index | INTEGER | 分段序号 |
  | file_path | TEXT | 相对路径 |
  | start_time | INTEGER (ms) | 该分段开始时间 |
  | end_time | INTEGER (ms) | 该分段结束时间 |
  | file_size | INTEGER | 字节数 |
  | upload_time | INTEGER (ms) | 上传时间 |

### 5.2 循环覆盖策略
**目标**：为每位员工保留最近 **N 个会话**（例如最近 20 个会话，每个会话包含若干分段），或总存储空间不超过配额（如每人 5GB）。

**实现**：每次新文件上传成功后，触发清理任务；每日凌晨定时全量扫描清理。

---

## 6. 管理后台集成

### 6.1 列表展示
管理员可在后台按员工、时间范围查询录制会话，列表展示每条会话的起止时间、分段数量、总大小等。

### 6.2 回放查看
采用 **自托管的 Playwright Trace Viewer**。后端提供签名下载 URL，前端通过 iframe 嵌入 `https://trace.company.com/viewer?trace=<signed-url>`。

---

## 7. 安全与权限
- **身份认证**：所有 API 请求需携带员工/管理员 Token（JWT）。
- **数据加密**：文件传输使用 HTTPS；静态文件可使用 AES-256 加密存储（可选）。
- **访问审计**：记录管理员查看录制的日志（谁、何时、查看哪个会话）。
- **员工同意**：部署前需获得员工知情同意，符合企业合规。

---

## 8. 性能优化与异常处理
- **文件压缩**：Tracing 本身生成 ZIP 文件，无需额外压缩。
- **网络上传**：异步上传，不阻塞录制；失败时重试（指数退避），并记录本地缓存以便后续补传。
- **磁盘空间**：代理本地临时文件应及时删除；服务器端定期清理。
- **浏览器崩溃**：代理监听浏览器断开事件，自动重连并恢复录制。
- **分段衔接**：停止和启动之间可能有几百毫秒的间隙，属于正常误差。

---

## 9. 实施路线图

| 阶段 | 任务 |
|------|------|
| **POC** | 在测试环境验证录制、分段、上传、回放功能。 |
| **优化** | 调整分段时长、清理策略参数，评估文件大小。 |
| **试点** | 选择少数员工试用，收集反馈。 |
| **全量部署** | 推广至所有员工，提供管理员培训。 |

---

## 10. 总结
本方案利用 Playwright Tracing 的轻量级截图能力，结合 **30 分钟分段录制** 和 **循环覆盖**，实现了对员工操作的长效监控，同时控制了存储成本。管理员可通过集成的 Trace Viewer 直观回放操作过程，满足审计与培训需求。
